import './Form.scss';

function Form() {
  return (
    <form className="form">
      <input className="form__input" />
      <button type="submit" className="form__button">
        &gt;
      </button>
    </form>
  );
}

export default Form;
